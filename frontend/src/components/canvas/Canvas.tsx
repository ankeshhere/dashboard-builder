import { Box } from "@mui/material";

export default function Canvas() {
  return (
    <Box
      sx={{
        flex: 1,
        bgcolor: "#f3f5f7",
        display: "flex",
        alignItems: "center",
        justifyContent: "center"
      }}
    >
      Dashboard Canvas
    </Box>
  );
}